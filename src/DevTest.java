import java.util.Scanner;

public class DevTest {
    public static void main(String[] args) {
        Scanner pipe = new Scanner(System.in);
        SafeInput.prettyHeader(pipe, "Enter a phrase to make pretty!");
    }
}
